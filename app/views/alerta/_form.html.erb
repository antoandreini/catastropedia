<%= form_with(model: alertum, local: true) do |form| %>
  <% if alertum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(alertum.errors.count, "error") %> prohibited this alertum from being saved:</h2>

      <ul>
      <% alertum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :titulo %>
    <%= form.text_field :titulo %>
  </div>

  <div class="field">
    <%= form.label :zonas %>
    <%= form.text_field :zonas %>
  </div>

  <div class="field">
    <%= form.label :fecha %>
    <%= form.datetime_select :fecha %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_area :descripcion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
